---
title: "World Happiness Report"
author: "Natalia Maldonado"
date: "`r Sys.Date()`"
format: revealjs
theme: white
transition: fade
revealjs:
  font: "Open Sans, Helvetica, Arial, sans-serif"
  css: |
    /* Estilo del título */
    .title-slide h1 {
      color: #ff6347;  /* Tomate (color vibrante) */
      font-size: 5em;  /* Título grande */
      font-weight: bold;
      text-align: center;  /* Centrado del título */
      text-transform: uppercase;  /* Texto en mayúsculas */
      letter-spacing: 6px;  /* Espaciado entre letras */
      text-shadow: 5px 5px 10px rgba(0, 0, 0, 0.2);  /* Sombra grande */
      margin-top: 150px;  /* Espacio desde arriba */
    }
    
    /* Estilo del subtítulo (fecha) */
    .title-slide .subtitle {
      font-size: 2em;
      color: #888888; /* Gris suave para la fecha */
      font-style: italic;
      text-align: center;  /* Centrado de la fecha */
      margin-top: 30px;  /* Espacio debajo del título */
    }
---
## Welcome to the World Happiness Report

This is a data visualization project presented on: `r Sys.Date()`

---
## Welcome to the World Happiness Report

This is a data visualization project presented on: `r Sys.Date()`


## Introducción

En esta presentación, exploraremos los datos del World Happiness Report.

## Datos

```{r}
# Cargar librerías
library(ggplot2)
library(dplyr)
library(readr) # Asegúrate de cargar esta librería

# Cargar datos
data_hapiness <- read_delim("data/data_hapiness.csv", delim = ";", escape_double = FALSE, trim_ws = TRUE)

```
## Quarto

Quarto enables you to weave together content and executable code into a finished presentation. To learn more about Quarto presentations see <https://quarto.org/docs/presentations/>.

## Bullets

When you click the **Render** button a document will be generated that includes:

-   Content authored with markdown
-   Output from executable code

## Code

When you click the **Render** button a presentation will be generated that includes both content and the output of embedded code. You can embed code like this:

```{r}
1 + 1
```
